mixin nav(className, items, links)
  nav(class=className)
    ul(class=className + "__list")
      each item, index in items
        li(class=className + "__item")
            a(class=className + "__link", href= links[index])
                div(class=className + "__text")= item